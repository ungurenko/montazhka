import CryptoKit

extension Digest {
    /// Шестнадцатеричная запись дайджеста: в таком виде он становится именем
    /// файла в кэше. Формат общий для всех кэшей — менять его можно только здесь.
    var hex: String {
        map { String(format: "%02x", $0) }.joined()
    }
}
